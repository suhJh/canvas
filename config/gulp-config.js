const src = 'src';
const dist = 'public';



module.exports = {
    src : {
        js : src + '/js',
        css : src + '/css',
        bower : dist + '/bower',
        html : dist
    },
    dist :{
        bundle : dist + '/js/build',
        js : dist + '/js',
        css : dist + '/css,',
        bower : dist + '/bower',
        html : dist
    }
};
